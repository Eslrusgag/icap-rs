services:
  squid:
    image: ubuntu/squid:latest
    ports:
      - "3128:3128"
    volumes:
      - ./squid.conf:/etc/squid/squid.conf:ro
    depends_on:
      - origin

  origin:
    image: nginx:alpine
    volumes:
      - ./origin:/usr/share/nginx/html:ro
